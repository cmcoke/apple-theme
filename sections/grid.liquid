<section>
  <div class="grid-container">

    {% comment %} 
            Check if the first image (image) setting is not blank. If it exists, render the container for the first image. 
        {% endcomment %}
    {%- if section.settings.image != blank -%}
      <div class="im-one">
        <h1></h1>
      </div>
    {% comment %} 
            If the first image is blank, render a default image container instead. 
        {% endcomment %}
    {%- else -%}
      <div class="def-image"></div>
    {%- endif -%}

    {% comment %} 
            Check if the second image (image2) setting is not blank. If it exists, render the container for the second image. 
        {% endcomment %}
    {%- if section.settings.image2 != blank -%}
      <div class="im-two">
        <h1></h1>
      </div>
    {% comment %} 
            If the second image is blank, render a default image container instead. 
        {% endcomment %}
    {%- else -%}
      <div class="def-image"></div>
    {%- endif -%}

    {% comment %} 
            Check if the third image (image3) setting is not blank. If it exists, render the container for the third image. 
        {% endcomment %}
    {%- if section.settings.image3 != blank -%}
      <div class="im-three">
        <h1></h1>
      </div>
    {% comment %} 
            If the third image is blank, render a default image container instead. 
        {% endcomment %}
    {%- else -%}
      <div class="def-image"></div>
    {%- endif -%}

    {% comment %} 
            Check if the fourth image (image4) setting is not blank. If it exists, render the container for the fourth image. 
        {% endcomment %}
    {%- if section.settings.image4 != blank -%}
      <div class="im-four">
        <h1></h1>
      </div>
    {% comment %} 
            If the fourth image is blank, render a default image container instead. 
        {% endcomment %}
    {%- else -%}
      <div class="def-image"></div>
    {%- endif -%}
  </div>
</section>


<style>
  .grid-container {
    display: grid;
    /* Creates two equal-width columns */
    grid-template-columns: repeat(2, 1fr);
    /* Creates two equal-height rows */
    grid-template-rows: repeat(2, 1fr);
    /* Optional gap between the grid items */
    gap: 10px;
    margin-bottom: 10px;
    padding-bottom: 10px;
  }

  .def-image {
    background-image: url("{{ 'ten.jpg' | asset_url }}");
    background-size: cover;
    background-position: center;
    width: 100%;
    height: 100%;
  }
  .im-one {
    background-image: url("{{ section.settings.image | image_url }}");
    background-size: cover;
    background-position: center;
    width: 100%;
    height: 100%;
  }
  .im-two {
    background-image: url("{{ section.settings.image2 | image_url }}");
    background-size: cover;
    background-position: center;
    width: 100%;
    height: 100%;
  }
  .im-three {
    background-image: url("{{ section.settings.image3 | image_url }}");
    background-size: cover;
    background-position: center;
    width: 100%;
    height: 100%;
  }
  .im-four {
    background-image: url("{{ section.settings.image4 | image_url }}");
    background-size: cover;
    background-position: center;
    width: 100%;
    height: 100%;
  }

  /* Optional: If a fixed height for each image is needed */
  .grid-container {
    height: 800px;
  }
</style>

{% comment %} 
  
  "name": The internal name of the section, shown in the Shopify theme editor.
  
  "settings": An array of configurable options for this section. 
    - "type": Defines the type of input field shown in the editor (e.g., "image_picker" allows users to upload/select an image).
    - "id": A unique identifier used to access this setting in Liquid code (e.g., section.settings.image).
    - "label": The label displayed next to the input field in the Shopify editor.
    - "info": Optional helper text shown below the input field to guide the user, such as recommended image size.
  
  "presets": Defines the default configuration when this section is first added to a page.
    - "name": The name of the section preset, shown in the theme editor’s "Add section" panel.
  
{% endcomment %}
{% schema %}
  {
    "name": "grid",
    "settings": [

      {
        "type": "image_picker",
        "id": "image",
        "label": "image",
        "info": "400 x 120px .png recommended"
      }, {
        "type": "image_picker",
        "id": "image2",
        "label": "image2",
        "info": "400 x 120px .png recommended"
      }, {
        "type": "image_picker",
        "id": "image3",
        "label": "image3",
        "info": "400 x 120px .png recommended"
      }, {
        "type": "image_picker",
        "id": "image4",
        "label": "image4",
        "info": "400 x 120px .png recommended"
      }


    ],
    "presets": [
      {
        "name": "grid"
      }
    ]
  }
{% endschema %}